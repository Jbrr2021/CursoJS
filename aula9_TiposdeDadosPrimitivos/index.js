//Tipos de dados primitivos String, number, underfined, null, boolean.

const nome = 'João' // string
const nome1 = "João" // string
const nome2 = `João` //string
const num1 = 10 // number
const num2 = 10.52 // number
let nomeAluno //undefined -> não aponta para local nenhuma na memória.
const sobrenomeAluno = null // Nulo -> não aponta para local nenhum na memória.
const boolean = true // valores boolean = true, false (valor lógico)
